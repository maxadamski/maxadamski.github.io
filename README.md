# maxadamski.com / max.adamski.xyz

My website

## How to build

Requirements: pug-cli (in path), markdown-it

```
npm install pug-cli -g  # install pug command line tool
npm install             # install node packages
make build              # render html files
```
